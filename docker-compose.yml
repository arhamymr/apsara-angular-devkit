version: '3.8'

services:
  demo:
    build: .
    container_name: apsara-demo
    ports:
      - "4200:4200"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:4200"]
      interval: 30s
      timeout: 10s
      retries: 3
